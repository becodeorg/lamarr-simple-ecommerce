{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
    <ul>
    {% for category in categories %}
        <li><a href="{{ path('category', {'category': category.id}) }}">{{ category.name }}</a></li>
    {% endfor %}
    </ul>
{% endblock %}
